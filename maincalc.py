import add
import sub
import mul
import div

a=100
b=50


total_sum = add.add(a,b)
total_sub = sub.sub(a,b)
total_mul = mul.mul(a,b)
total_div = div.div(a,b)

print(f"total sum is {total_sum}. total sub is {total_sub}. total mul is{total_mul}. total div is{total_div}")